# Required
mcp>=1.2.0

# Optional (graceful degradation when missing)
# - anthropic: LLM-based tagging / cluster summaries
# - voyageai:  high-quality embeddings
# - numpy:     vector math acceleration (falls back to pure Python)
anthropic>=0.39.0
voyageai>=0.3.0
numpy>=1.26.0
