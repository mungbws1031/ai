# 에이전트 요구사항 + MVP 초안

아래는 바로 개발에 들어갈 수 있도록 작성한 **실전용 1장 기획서**입니다.

## 1) 에이전트 목표
- 매일 오전 9시에 사용자의 캘린더와 미확인 메일을 요약해,
  **오늘의 우선순위 할 일 Top 5**를 제안하는 개인 업무 보조 에이전트를 만든다.

## 2) 사용자
- 개인/소규모 팀의 지식노동자
- 캘린더, 메일, 메모 툴을 쓰지만 할 일 우선순위화에 어려움이 있는 사용자

## 3) 입력 데이터
- Google Calendar: 오늘 일정 목록(시간, 제목, 참석자)
- Gmail: 읽지 않은 메일(발신자, 제목, 수신시각)
- 사용자 자유 텍스트: "오늘은 회의가 많아서 집중시간이 필요해" 같은 컨텍스트

## 4) 사용 가능한 도구
- Calendar API (읽기 전용)
- Gmail API (읽기 전용)
- (선택) Notion/메모 API (읽기 전용)

## 5) 출력 형식
- 섹션 A: 오늘 핵심 일정 3줄 요약
- 섹션 B: 긴급 메일 후보 3개
- 섹션 C: 우선순위 할 일 Top 5 (이유 포함)
- 섹션 D: 25분 집중 블록 2개 추천 시간

## 6) 성공 기준 (MVP)
- 정확도: 사용자가 체감한 "우선순위가 합리적" 평가 5점 만점 중 평균 4.0 이상
- 시간: 요청 후 10초 이내 결과 제공
- 안정성: 평일 5일 중 4일 이상 자동 실행 성공

## 7) 금지/주의 사항
- 메일/일정 원문을 외부 로그에 저장 금지
- API 토큰은 환경변수로만 관리, 코드/문서 하드코딩 금지
- 일정 삭제/수정 같은 쓰기 동작은 MVP에서 제외
- 민감정보(계좌, 주민번호, 비밀번호) 감지 시 즉시 마스킹

## 8) MVP 범위 (2주)

### In-scope
1. 수동 트리거(버튼/명령어)로 요약 실행
2. 캘린더 + 메일 읽기 통합
3. Top 5 우선순위 생성 규칙(시간 임박도, 발신자 중요도, 사용자가 준 맥락)
4. 결과를 고정 템플릿으로 출력

### Out-of-scope
- 일정 자동 재배치
- 메일 자동 회신 발송
- 다중 계정 통합

## 9) 구현 순서
1. FastAPI 기본 서버 + `/plan-today` 엔드포인트 생성
2. Calendar/Gmail 읽기 클라이언트 연결
3. LLM 프롬프트(요약 + 우선순위 선정) 구성
4. 실패 처리(데이터 없음/권한 오류/타임아웃)
5. 결과 로그(요청시각, 지연시간, 성공/실패 코드) 적재

## 10) 검증 시나리오 (샘플 10개)
- 회의가 많은 날
- 메일 폭주 날
- 일정이 거의 없는 날
- API 권한 만료
- 데이터 0건

각 시나리오에서 아래를 기록한다.
- 완료 여부
- 응답 시간
- 사용자 만족도(1~5)
- 오류 원인 및 재발 방지 규칙

## 11) 다음 액션
- 원하면 이 문서를 기준으로 바로 다음 단계까지 작성 가능:
  1. FastAPI 프로젝트 폴더 구조
  2. `.env.example` 및 보안 설정
  3. `/plan-today` 최소 동작 코드 스켈레톤
