# AI 에이전트 만들기 시작 가이드

`AI 에이전트`는 단순 챗봇이 아니라 **목표를 받아 계획하고, 도구를 사용해, 결과를 검증하는 실행 시스템**입니다.

## 1) 먼저 목적을 한 줄로 정의하세요

예시:
- "매일 아침 내 캘린더와 메일을 읽고 오늘 할 일을 우선순위로 정리해 주는 에이전트"
- "쇼핑몰 문의를 분류하고, 환불/교환/배송 답변 초안을 자동 생성하는 에이전트"

목적이 좁고 명확할수록 성공 확률이 크게 올라갑니다.

## 2) 최소 기능(MVP)부터 설계하세요

초기 MVP는 아래 4가지로 충분합니다.

1. **입력**: 사용자 요청 받기
2. **추론**: LLM으로 의도/계획 생성
3. **도구 실행**: 검색, DB 조회, API 호출 등
4. **검증/출력**: 결과 확인 후 답변

## 3) 에이전트의 핵심 구조

- **Planner(계획기)**: 해야 할 일을 작은 단계로 쪼갬
- **Executor(실행기)**: 단계별로 적절한 도구 호출
- **Memory(메모리)**: 대화/작업 맥락 저장(짧은 메모리 + 장기 저장)
- **Guardrail(안전장치)**: 금지 작업, 민감정보, 실패 시 중단 규칙
- **Evaluator(평가기)**: 결과 품질/정확도 점검

## 4) 빠른 기술 스택 예시

- 모델: GPT 계열 또는 목적별 모델
- 백엔드: Python(FastAPI) 또는 Node.js
- 오케스트레이션: 간단할수록 직접 코드로 구현(초기엔 프레임워크 과용 금지)
- 저장소: Redis(단기 상태), Postgres/Vector DB(지식 저장)

## 5) 초반에 꼭 넣어야 할 안전장치

- 도구별 권한 분리(읽기/쓰기/삭제)
- 프롬프트 인젝션 방어 규칙
- 민감정보 마스킹(토큰, 주민번호, 카드번호 등)
- 실패 시 재시도 횟수 제한 + 사용자 확인
- 모든 실행 로그 기록(누가/언제/무엇을 실행했는지)

## 6) 개발 순서 (현실적인 7단계)

1. 단일 업무 시나리오 1개 선정
2. 수동 처리 기준으로 정답 예시 20~50개 수집
3. "계획 → 도구 1개 호출 → 결과 요약" 파이프라인 구현
4. 실패 케이스를 규칙으로 보강
5. 자동 평가 스크립트(정확도/완료율/비용/지연시간) 추가
6. 사용자 3~5명에게 파일럿
7. 로그 기반으로 반복 개선

## 7) 실무 팁

- 처음부터 "완전 자율 에이전트"를 만들지 마세요. **반자동(co-pilot)**이 더 빨리 성공합니다.
- 프롬프트보다 **도구 설계와 데이터 품질**이 성능에 더 큰 영향을 줍니다.
- 비용 폭주를 막으려면 단계 수, 토큰 수, 재시도 수에 상한을 두세요.

## 8) 다음 액션

원하시면 아래 중 하나로 바로 시작할 수 있습니다.

1. 아이디어를 말해주시면 제가 **요구사항 문서 + MVP 설계**로 바꿔드릴게요.
2. 바로 코드를 원하면 **Python/FastAPI 기준 에이전트 템플릿**을 만들어드릴게요.
3. 회사/서비스용이면 **보안·운영 체크리스트**까지 포함해 드릴게요.

## 9) "그래서 지금 당장 뭐부터?" 실행 체크리스트

헷갈릴 때는 아래 순서로 바로 시작하면 됩니다.

1. **주제 1개 고르기 (10분)**
   - 예: "고객 문의 자동 분류", "내 일정 요약", "면접 질문 생성"
2. **성공 기준 2개 정하기 (10분)**
   - 예: 정확도 80% 이상, 응답 10초 이내
3. **도구 1개만 연결하기 (20분)**
   - 예: 구글 시트 조회, 사내 FAQ API 조회
4. **샘플 요청 10개로 돌려보기 (20분)**
   - 잘 되는 케이스/실패 케이스를 분리해 기록
5. **실패 규칙 3개 추가하기 (20분)**
   - 예: 정보 부족 시 재질문, 민감정보 감지 시 중단

위 5단계만 끝내도 "작동하는 첫 번째 에이전트"가 만들어집니다.

## 10) 요청 템플릿 (복붙용)

아래 형식으로 요구사항을 적으면, 바로 설계/구현으로 넘어가기 쉽습니다.

```text
[에이전트 목표]
어떤 문제를 해결하나요?

[사용자]
누가 쓰나요?

[입력 데이터]
예: 채팅 메시지, 이메일, CSV, DB

[사용 가능한 도구]
예: Notion API, 사내 DB 조회, 웹 검색

[성공 기준]
예: 정확도, 처리시간, 비용

[금지/주의 사항]
예: 개인정보 처리 금지, 삭제 동작은 항상 사용자 확인
```
